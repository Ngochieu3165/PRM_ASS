<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Exercise2Slot3" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Compose + Material3 handles colors; keep this file minimal to avoid resource linking issues -->
    </style>

    <style name="Toolbar.Title" parent="android:TextAppearance.Medium">
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">#55000000</item>
        <item name="android:shadowDx">2</item>
        <item name="android:shadowDy">2</item>
        <item name="android:shadowRadius">3</item>
    </style>

</resources>